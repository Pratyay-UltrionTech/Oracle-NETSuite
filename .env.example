# Environment variables are split by app — do not use a root .env file.
#
# Backend (FastAPI):
#   cp backend/.env.example backend/.env
#
# Frontend (Vite):
#   cp frontend/.env.example frontend/.env
