# Frontend Configuration (Vite)
# Local development — copy to frontend/.env
VITE_API_URL="http://localhost:8000/api"

# Production API URL is set in src/config/urls.ts (stage.atisunya.cloud).

# Optional
# GEMINI_API_KEY=