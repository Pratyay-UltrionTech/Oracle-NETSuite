# Frontend Configuration (Vite)
# Local development — copy to .env
VITE_API_URL="http://localhost:8000/api"

# Production URL is set in .env.production and the Azure SWA workflow.

# Optional
# GEMINI_API_KEY=
